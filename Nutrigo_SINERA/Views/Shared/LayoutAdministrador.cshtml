<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title - Panel del Administrador</title>
    <link href="~/source/css/bootstrap.min.css" rel="stylesheet" />
    <script src="~/source/js/jquery-3.7.1.min.js"></script>
    <script src="~/source/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar fijo del Administrador -->
            <div class="col-md-2 bg-light" style="min-height: 100vh; padding: 20px;">
                <h4 class="fw-bold">Nutrigo</h4>
                <ul class="nav flex-column mt-4">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Administrador")">🏫 Gestionar Colegio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Perfil", "Administrador")">👤 Editar Perfil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Notificaciones", "Administrador")">🔔 Notificaciones</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="@Url.Action("Index", "Home")">🚪 Cerrar Sesión</a>
                    </li>
                </ul>
            </div>

            <!-- Contenido -->
            <div class="col-md-10 p-4">
                @RenderBody()
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    @RenderSection("Scripts", required: false)

</body>
</html>
